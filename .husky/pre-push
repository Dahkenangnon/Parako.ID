# Strict validation runs on CI (.github/workflows/release.yml).
# Local pre-push is intentionally a no-op to keep dev push fast.
# For an ad-hoc strict pass before pushing, run `pnpm run verify`.
exit 0
